// Demonstration of ES6 Module Export (default export)

// Sample data
let data = [1, 2, 3];

// Sample string
let name = "raju";

// Exporting an object as default
// This allows importing modules to access both `data` and `name`
export default { data, name };
